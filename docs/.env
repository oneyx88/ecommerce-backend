# -------------------------------
# 🔐 Keycloak Admin Credentials
# -------------------------------
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# -------------------------------
# 🌐 Network & Host Configuration
# -------------------------------
KC_HOSTNAME=localhost
KC_HTTP_ENABLED=true
KC_HTTP_PORT=8080
KC_HTTPS_PORT=8443

# -------------------------------
# ⚙️ Database Configuration (Default: H2)
# 如果你后续使用 RDS 或 PostgreSQL，可修改为外部数据库
# -------------------------------
KC_DB=dev-file

# KC_DB=postgres
# KC_DB_URL=jdbc:postgresql://your-rds-endpoint:5432/keycloak
# KC_DB_USERNAME=keycloak
# KC_DB_PASSWORD=your_password

# -------------------------------
# 🧩 Realm Import (可选)
# 自动导入 Realm JSON 文件（若你使用 kc.sh export）
# -------------------------------
# KC_IMPORT=/opt/keycloak/data/import/ecomm-app-realm.json
# KC_IMPORT_STRATEGY=IGNORE_EXISTING

# -------------------------------
# 🛡️ HTTPS (如果未来启用证书)
# -------------------------------
# KC_PROXY=edge
# KC_HTTPS_CERTIFICATE_FILE=/etc/x509/https/tls.crt
# KC_HTTPS_CERTIFICATE_KEY_FILE=/etc/x509/https/tls.key

# -------------------------------
# 🧠 Logging & Startup
# -------------------------------
KC_LOG_LEVEL=info
KC_HEALTH_ENABLED=true
KC_METRICS_ENABLED=true
