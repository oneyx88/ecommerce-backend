apiVersion: v1
kind: Service
metadata:
  name: redisinsight
  labels:
    app: redisinsight
spec:
  type: NodePort   # ✅ 允许浏览器外部访问
  selector:
    app: redisinsight
  ports:
    - port: 5540
      targetPort: 5540
      nodePort: 30540   # ✅ 可选固定端口（访问 <NodeIP>:30540）


